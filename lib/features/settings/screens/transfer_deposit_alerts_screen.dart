import 'package:flutter/material.dart';

class TransferDepositAlertsScreen extends StatelessWidget {
    const TransferDepositAlertsScreen({super.key});

    @override
    Widget build(BuildContext context) {
        return Scaffold(
            appBar: AppBar(title: const Text('Transfer & Deposit Alerts')),
            body: const Center(child: Text('Notification channels & rules (TBD)')),
        );
    }
}
